function openPopover(){
    // let snackbar = document.getElementById('snackbar');
    // snackbar.style.animation = 'slideShow 3s';
    // snackbar.style.animationDelay = '2s';
    // snackbar.style.animationFillMode = 'forwards';
}

function closePopover(){
    let snackbar = document.getElementById('snackbar');
    snackbar.style.animation = 'slideHide 3s';
}